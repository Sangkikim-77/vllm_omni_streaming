[project]
name = "vllm-omni-sample-client"
version = "0.1.0"
description = "Sample HTTPS web client for vLLM-Omni"
requires-python = ">=3.10"
dependencies = [
  "pyyaml>=6.0",
]

[build-system]
requires = ["hatchling>=1.24.0"]
build-backend = "hatchling.build"

[tool.uv]
package = false
